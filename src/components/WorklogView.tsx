export function WorklogView() {
  return (
    <div className="worklog">
      <h2>AI Worklog</h2>
      <p className="worklog__intro">
        Честный отчёт о процессе разработки — какие AI-инструменты использовались,
        что делал человек, что делал AI, и что бы стоило доработать.
      </p>

      <section>
        <h3>Инструменты</h3>
        <ul>
          <li>Claude Code (CLI-агент Anthropic) — единственный инструмент разработки в этой сессии.</li>
          <li>
            Модель менялась по ходу сессии в рамках линейки Claude 5: обсуждение задания и планирование
            велись на Claude Fable 5.1, реализация — на Claude Sonnet 5.
          </li>
        </ul>
      </section>

      <section>
        <h3>Время и токены</h3>
        <p>
          Секундомер отдельно не запускался — Claude Code не показывает суммарное время сессии как
          готовую метрику. Вся работа сделана за один непрерывный присест 13 сентября 2026 года
          (от чтения ТЗ до финальной проверки и деплоя).
        </p>
        <p>
          Классической метрики «сколько токенов API потрачено» инструмент в этой сессии не показывает.
          Единственное, что видно — счётчик оставшегося контекстного бюджета сессии, который за всё
          время работы уменьшился примерно на 60–70 тысяч условных единиц. Это отражает объём
          контекста диалога, а не биллинговые input/output-токены, поэтому честно указываю: точных
          токенов не считала, инструмент эту цифру в привычном виде не отдаёт.
        </p>
      </section>

      <section>
        <h3>Этапы работы</h3>
        <ol>
          <li>
            <strong>Идея и уточнение ТЗ.</strong> Человек прислал оба варианта задания MOX целиком и
            попросил AI разобрать, что именно проверяется. AI сравнил структуру обоих вариантов и выделил
            общие требования (объяснимый алгоритм, invalid-состояния, ручной override, отчёт, Worklog в
            интерфейсе, тесты).
            <br />
            <em>Человек:</em> ответил на уточняющие вопросы и принял три решения — вариант («Бюро
            переселения»), стек (React + TypeScript + Vite без бэкенда), площадка деплоя (Vercel).
            <em> AI:</em> провёл анализ ТЗ и сформулировал вопросы.
          </li>
          <li>
            <strong>Архитектура и план.</strong> В режиме планирования AI изучил структуру рабочего
            окружения, чтобы не смешать новый проект с уже существующим репозиторием другой задачи
            (CCA-F), выбрал отдельную папку под новый git-репозиторий, спроектировал модель данных,
            разделил алгоритм на жёсткие ограничения (дисквалифицируют место) и мягкий скоринг
            (ранжирует оставшихся кандидатов), расписал обязательные состояния и план тестов.
            <br />
            <em>Человек:</em> принял план без правок (одобрение на уровне архитектуры, не построчно).
            <em> AI:</em> спроектировал и написал весь план.
          </li>
          <li>
            <strong>Логика подбора (TDD).</strong> Сначала были написаны unit-тесты на алгоритм подбора
            и на построение отчёта, затем — сама реализация (<code>matching.ts</code>, <code>report.ts</code>).
            Изначальные 15 тестов прошли с первого запуска; позже добавлены ещё — сейчас в проекте 22
            unit-теста (<code>npm run test</code>).
            <br />
            <em>Человек:</em> не вмешивался построчно — решение принято на уровне одобренного плана.
            <em> AI:</em> написал тесты и реализацию.
          </li>
          <li>
            <strong>Состояние приложения и UI.</strong> AI реализовал стейт-менеджмент на
            <code>useReducer</code> с сохранением в <code>localStorage</code>, формы добавления заявок и мест
            с клиентской валидацией, карточки подбора с предупреждением при конфликте ручного выбора,
            отчёт и вкладку Worklog.
            <br />
            <em>Человек:</em> не вмешивался построчно — та же логика, что и на предыдущем этапе.
            <em> AI:</em> написал весь код этого этапа.
          </li>
          <li>
            <strong>Отладка.</strong> Первая сдача ушла с реальным багом в пересчёте назначений (см.
            ниже) — ни тесты, ни ручная проверка на тот момент его не поймали.
            <br />
            <em>Человек:</em> инициировал независимый внешний аудит проекта уже после первой сдачи — он и
            выявил баг (воспроизведён на проде).
            <em> AI:</em> исправил баг отдельным коммитом и добавил регрессионный тест.
          </li>
          <li>
            <strong>Финальная проверка.</strong> Прогон тестов и сборки, ручное прохождение сценариев из
            README-чеклиста, деплой на Vercel.
            <br />
            <em>Человек:</em> утвердил результат перед сдачей.
            <em> AI:</em> прогнал тесты/сборку и чек-лист.
          </li>
        </ol>
      </section>

      <section>
        <h3>Ключевые промпты (по сути, не дословно)</h3>
        <ul>
          <li>«Изучи оба варианта задания и скажи вывод, чего они хотят» — на этапе анализа ТЗ.</li>
          <li>Ответы на уточняющие вопросы: выбор варианта, стека и площадки деплоя.</li>
          <li>Одобрение плана реализации без изменений (ExitPlanMode → approve).</li>
          <li>Далее разработка велась автономно AI по одобренному плану без дополнительных промптов на
            каждый файл — человек контролировал решение на уровне плана, а не построчно.</li>
        </ul>
      </section>

      <section>
        <h3>Решения, принятые человеком</h3>
        <ol>
          <li>Выбор темы — «Бюро переселения привидений», а не «Лаборатория порталов».</li>
          <li>Стек — React + TypeScript + Vite без отдельного бэкенда.</li>
          <li>Площадка деплоя — Vercel.</li>
          <li>Принятие предложенной AI архитектуры (модель данных, разделение hard/soft-ограничений,
            структура из трёх вкладок) без запроса альтернатив.</li>
        </ol>
      </section>

      <section>
        <h3>Где AI ошибся</h3>
        <p>
          В первой версии <code>recomputeAutoAssignments</code> ручные назначения добавлялись в
          аккумулятор «по ходу» перебора заявок в их исходном порядке, а не все сразу в начале. Из-за
          этого при пересчёте авто-подбора для заявки, стоящей раньше в списке, ещё не были видны
          ручные назначения на более поздние заявки — алгоритм мог не заметить, что место уже занято
          вручную, и превысить вместимость. Эта ошибка не была поймана AI при первой сдаче — она осталась
          в проде и была найдена внешним аудитом (воспроизведено: «Старый маяк» 1/2, идеально подходящее
          место, а заявка при этом ошибочно показывала «Нет доступных мест»). После этого замечания AI
          исправил баг и добавил регрессионный тест (<code>src/state/AppStateContext.test.ts</code>) —
          это честный пример того, где сгенерированный код прошёл первую сдачу с дефектом, который сам не
          заметил.
        </p>
      </section>

      <section>
        <h3>Что было переписано/доработано вручную</h3>
        <p>
          В этой сессии весь код написан AI-агентом (Claude Code) по согласованному с человеком плану;
          человек не редактировал код построчно, а управлял на уровне решений и приёмки плана. Это
          честное ограничение данной демонстрации: в реальном рабочем процессе на этом месте должно быть
          построчное код-ревью человеком, а не только приёмка на уровне архитектуры.
        </p>
      </section>

      <section>
        <h3>Как проверялась работоспособность</h3>
        <ul>
          <li>Автотесты: <code>npm run test</code> — юнит-тесты на алгоритм подбора и отчёт.</li>
          <li>Сборка: <code>npm run build</code> — проверка отсутствия ошибок типов и сборки.</li>
          <li>Ручное прохождение 5 сценариев из README (пустой список, невозможное переселение,
            переполненное место, конфликтующий ручной выбор, ошибка ввода).</li>
        </ul>
      </section>

      <section>
        <h3>Что доработать в реальном продукте</h3>
        <ul>
          <li>Построчное код-ревью человеком, а не только приёмка плана.</li>
          <li>Бэкенд с persistence в базе данных вместо localStorage, многопользовательский доступ.</li>
          <li>Настраиваемые веса алгоритма подбора (сейчас захардкожены) и админ-панель для их тюнинга.</li>
          <li>История изменений по каждой заявке (сейчас видно только текущее назначение).</li>
          <li>E2E-тесты (Playwright) поверх текущих unit-тестов логики.</li>
          <li>Локализация интерфейса (сейчас только русский).</li>
        </ul>
      </section>
    </div>
  );
}
